document.addEventListener('DOMContentLoaded', function () {
    const backgroundImage = document.querySelector('.background-image');

    // Slide the background image into position when page is loaded
    backgroundImage.style.top = '0';
    
});
